{
    "name": "f1-telemetry",
    "private": true,
    "scripts": {
        "dev:web": "pnpm --filter web dev",
        "dev:ingestor": "npm run dev --workspace services/telemetry-ingestor",
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "typecheck": "pnpm -r typecheck"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "react": "^19.3.0",
        "react-dom": "^19.3.0",
        "web": "^0.0.2"
    }
}
